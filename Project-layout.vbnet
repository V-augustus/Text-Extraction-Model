Text-Extraction-Model/
│
├── src/
│   ├── preprocessing.py        ← main preprocessing code (we’ll modify this)
│   ├── __init__.py             ← optional, marks src as a package
│
├── images/
│   ├── image1.png              ← sample image for testing
│
├── data/
│   ├── sample_preprocessed.png ← preprocessed output (auto-generated)
│
├── tests/
│   ├── test_preprocessing.py   ← new file for automated testing
│
├── preprocessing_demo.ipynb    ← new Jupyter Notebook for visualization
│
├── requirements.txt            ← (optional) list of dependencies
└── README.md                   ← (optional) documentation for teammates
